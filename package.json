{
  "name": "desymmetrical-core-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build:spatial": "npm --prefix spatial-study run build",
    "test:spatial": "npm --prefix spatial-study test"
  }
}
